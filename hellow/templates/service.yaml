apiVersion: v1
kind: Service
metadata:
  name: {{ template "hellow.fullname" . }}
  labels:
     app.kubernetes.io/name: {{ template "hellow.fullname" . }}
     component: {{ template "hellow.fullname" . }}
     release: {{ .Release.Name }}
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app.kubernetes.io/name: {{ template "hellow.fullname" . }}

